import test from 'ava';
import execa from 'execa';

test('makes new timecard', async t => {
	t.true(false);
});

test('clockin', t => {
	t.true(false);
});

test('clockout', t => {
	t.true(false);
});

test('print', t => {
	t.true(false);
});

